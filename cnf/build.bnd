version=$(cat;${workspace}/version)
apiVersion=$(cat;${workspace}/apiVersion)
cache=${.}/cache

# JUnit
junit: \
  junit-jupiter,\
  junit-jupiter-api
mockito: org.mockito.mockito-all

Bundle-Version:             ${version}.${tstamp;yyyyMMdd-HHmmss}

-remoteworkspace        true
-runfw:                 org.eclipse.osgi
-runee:                 JavaSE-11

Automatic-Module-Name: ${project.name}
